// empty wrapper for child elements
// used when we want adjacent elements without extra wrapper element in real DOM
const aux = (props) => props.children;

// React 16.2 uses React.Fragment

export default aux;
